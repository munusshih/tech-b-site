---
import { getYearConfig } from "@/config";

interface Props {
  year: number;
}

const { year } = Astro.props;
const yearConfig = getYearConfig(year);
---

<aside class="archive-notice" role="status">
  <strong>{yearConfig.semester || year}</strong>
  <span>Archived · Read-only</span>
</aside>
